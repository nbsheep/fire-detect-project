# 火灾 / 烟雾检测 —— 从零到跑通 完整实操手册

> 目标:用无人机相机画面识别 **火焰** 和 **烟雾**,框出来并给出置信度,
> 最终能接视频流做实时检测,为"自主巡检 + 异常报告"打底。
> 技术栈:Python + PyTorch + Ultralytics YOLO(目标检测)。

---

## 0. 先读这里:整体路线

咱们分 6 步走,**每一步跑通了再进下一步**,不要跳:

| 步骤 | 做什么 | 产出 |
|------|--------|------|
| 1 | 环境搭建 | 一个能用 GPU 的 Python 环境 |
| 2 | 拿数据 | 已标注的火/烟数据集 |
| 3 | 训练 | 训练好的模型权重 `best.pt` |
| 4 | 评估 | 准确率指标 + 预测效果图 |
| 5 | 推理 | 对图片/视频/摄像头做检测 |
| 6 | 扩展 | 接无人机流 + 自动报告(后续) |

> ⚠️ 手册里所有命令都在 **Git Bash** 里执行(你系统里已装 git bash)。
> 如果你更习惯用 CMD / PowerShell,把路径里的 `/` 换成 `\` 即可,命令本身一样。

---

## 1. 你的机器情况(已探明,无需再查)

- **显卡:NVIDIA RTX 5060 Ti,16GB 显存** → 能本地训练,够用。
- **架构:Blackwell(50 系)** → 必须装 **CUDA 12.8 版**的 PyTorch,老版本跑不起来。这是最容易踩的坑。
- **conda 已装**、**git 已装**。
- **注意**:你系统里有好几个 Python(3.13 和 3.10 混着),直接用会乱。
  所以我们用 conda 单独建一个干净环境,**不碰系统 Python**。

---

## 2. 步骤一:环境搭建

### 2.1 创建干净的 conda 环境

```bash
conda create -n fire-detect python=3.11 -y
```

> 说明:建一个叫 `fire-detect` 的独立环境,Python 用 3.11(和 YOLO 兼容最稳)。
> 这一步我之前已经帮你建好了,如果提示已存在,直接进下一步。

### 2.2 记住这个环境的 python 路径

后面所有命令都用这个**绝对路径**来调,避免激活失效的问题:

```
C:/Users/nice/anaconda3/envs/fire-detect/python.exe
```

> 为省事,下面统一用 `$PY` 代替它。在 Git Bash 里先执行这行,当前窗口就能用 `$PY`:
> ```bash
> PY="C:/Users/nice/anaconda3/envs/fire-detect/python.exe"
> ```
> ⚠️ 关掉窗口后要重新执行这行。

### 2.3 装 PyTorch(CUDA 12.8 版 —— 关键步骤)

```bash
$PY -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
```

> - 这个包很大(2-3GB),下载要几分钟到十几分钟,取决于网速,**耐心等**。
> ✅ **这一步已经完成了**(torch 2.11.0+cu128,CUDA 已验证可用),你可以直接跳到 2.4。
>   下面的命令留作参考:如果哪天要重装,必须带 `--index-url ...cu128`,
>   否则装成 CPU 版或不兼容 50 系显卡的版本。

### 2.4 装 Ultralytics(YOLO 框架)

```bash
$PY -m pip install ultralytics
```

> 这个会顺带装 opencv、numpy 等依赖。它**不会**动到上面的 torch(已经装好了)。

### 2.5 验证环境(非常重要,务必通过再往下)

新建文件 `scripts/check_env.py`,内容:

```python
import torch
print("PyTorch 版本 :", torch.__version__)
print("CUDA 是否可用:", torch.cuda.is_available())   # 必须是 True
if torch.cuda.is_available():
    print("显卡型号    :", torch.cuda.get_device_name(0))
    print("显存(GB)   :", round(torch.cuda.get_device_properties(0).total_memory/1e9, 1))
```

运行:

```bash
$PY scripts/check_env.py
```

**期望输出**(关键是第二行 `True`):

```
PyTorch 版本 : 2.x.x+cu128
CUDA 是否可用: True
显卡型号    : NVIDIA GeForce RTX 5060 Ti
显存(GB)   : 16.3
```

> ❌ 如果 `CUDA 是否可用: False`:说明 torch 装错版本了。
> 执行 `$PY -m pip uninstall torch torchvision -y` 卸掉,重新跑 2.3(注意 cu128)。

---

## 3. 步骤二:拿数据(D-Fire 数据集)

我们用公开的 **D-Fire** 数据集:约 **21,000 张**图片,已经是 YOLO 格式标注,
两个类别:**烟雾(smoke)** 和 **火焰(fire)**。省掉自己采集+标注的痛苦。

### 3.1 下载

D-Fire 的官方地址(GitHub 仓库,里面有网盘下载链接):

```
https://github.com/gaiasd/DFireDataset
```

操作:
1. 打开上面的链接,按 README 里的 Google Drive / OneDrive 链接下载压缩包。
2. 解压到 `C:/Users/nice/fire-detect-project/data/dfire/`。

解压后目录应该长这样(可能层级略有不同,以实际为准):

```
data/dfire/
├── train/
│   ├── images/        # 训练图片 .jpg
│   └── labels/        # 训练标注 .txt(每行: 类别id x y w h)
└── test/
    ├── images/
    └── labels/
```

> 💡 如果下载慢或打不开,告诉我,我给你换 Kaggle / Roboflow 上的镜像版本。
> Roboflow 上搜 "fire smoke" 也有很多现成的 YOLO 格式数据集,可作备选。

### 3.2 检查标注的类别顺序(别跳过)

不同数据集的类别 id 顺序可能不一样。随便打开一个 `labels/` 里的 `.txt`,
每行第一个数字是类别 id(0 或 1)。D-Fire 的约定是:

- `0` = smoke(烟雾)
- `1` = fire(火焰)

**记住这个顺序**,下一步写配置文件要对应上。如果你下的是别的数据集,以它自己的说明为准。

### 3.3 写数据集配置文件

新建 `data/dfire.yaml`,内容(注意 `path` 用你解压后的实际路径):

```yaml
# 数据集根目录(绝对路径,反斜杠要写成正斜杠)
path: C:/Users/nice/fire-detect-project/data/dfire

# 相对 path 的子目录
train: train/images
val: test/images      # 没有单独的验证集时,先用 test 当验证集

# 类别:顺序必须和标注里的 id 对应
names:
  0: smoke
  1: fire
```

> 这个 yaml 是 YOLO 训练的"地图",告诉它图在哪、有几类、每类叫什么。写错路径是最常见的报错来源。

---

## 4. 步骤三:训练

### 4.1 训练脚本

新建 `scripts/train.py`:

```python
from ultralytics import YOLO

# 用官方预训练的 yol11n(nano,最小最快),在它基础上微调 —— 这叫迁移学习,
# 比从零训练快得多、效果好得多。第一次运行会自动下载 yolo11n.pt。
model = YOLO("yolo11n.pt")

if __name__ == "__main__":
    model.train(
        data="data/dfire.yaml",   # 上一步写的配置
        epochs=50,                 # 训练轮数,先 50,不够再加
        imgsz=640,                 # 输入图片尺寸,640 是标准值
        batch=16,                  # 一批多少张,16 对 16G 显存很稳;爆显存就调小到 8
        device=0,                  # 用第 0 块 GPU;写 "cpu" 则用 CPU(很慢)
        workers=4,                 # 数据加载线程,Windows 上别调太高
        project="runs",            # 输出目录
        name="fire_yolo11n",       # 本次训练的名字
        patience=15,               # 15 轮没提升就早停,省时间
    )
```

### 4.2 开始训练

```bash
$PY scripts/train.py
```

> - `if __name__ == "__main__"` 这层保护在 **Windows 上必须有**,否则多进程数据加载会报错。
> - 训练过程中终端会滚动显示每轮的 loss 和精度指标(box_loss、mAP 等)。
> - 用 `yolo11n` + 你的 5060 Ti,50 轮大约 **1-2 小时**(21000 张图)。想先快速验证流程,
>   把 `epochs` 改成 `3` 跑一遍,看看能不能顺利跑完、有没有报错。
> - 训练时可以另开一个终端跑 `nvidia-smi` 看 GPU 有没有在干活(利用率、显存占用应明显上升)。

### 4.3 训练产出在哪

训练完,结果都在:

```
runs/fire_yolo11n/
├── weights/
│   ├── best.pt      # ★ 最好的模型权重(后面推理就用它)
│   └── last.pt      # 最后一轮的权重
├── results.png      # 训练曲线(loss 下降、mAP 上升)
├── confusion_matrix.png   # 混淆矩阵
└── val_batch0_pred.jpg    # 验证集预测效果图(直观看框准不准)
```

> 先打开 `results.png` 和 `val_batch0_pred.jpg` 看一眼:曲线在收敛、框框大致框对了,就说明训练成功。

### 4.4 常见报错速查

| 报错关键字 | 原因 | 解决 |
|-----------|------|------|
| `CUDA out of memory` | 显存不够 | `batch` 调小(16→8→4) |
| `dataset ... not found` | yaml 路径错 | 检查 `data/dfire.yaml` 的 `path` |
| `An attempt has been made to start a new process...` | 少了 `__main__` 保护 | 见 4.1 的写法 |
| 训练极慢、GPU 利用率 0 | 在用 CPU | 确认 `device=0` 且第 2.5 步验证过 CUDA=True |

---

## 5. 步骤四:评估

看模型到底行不行,靠指标和图,不靠感觉。

### 5.1 跑评估

新建 `scripts/eval.py`:

```python
from ultralytics import YOLO

model = YOLO("runs/fire_yolo11n/weights/best.pt")

if __name__ == "__main__":
    metrics = model.val(data="data/dfire.yaml", imgsz=640, device=0)
    print("mAP50    :", round(metrics.box.map50, 4))   # 越接近 1 越好
    print("mAP50-95 :", round(metrics.box.map, 4))
```

```bash
$PY scripts/eval.py
```

### 5.2 怎么看指标

- **mAP50**:最常看的指标。> 0.6 算能用,> 0.8 算不错。火/烟这种目标,烟往往比火难检(边界模糊)。
- 如果某一类特别差,多半是那一类的数据少,或标注质量差,后面可以针对性补数据。

---

## 6. 步骤五:推理(见效果)

### 6.1 检测单张图片

先随便找一张有火/烟的图片放到 `samples/test.jpg`。新建 `scripts/predict_image.py`:

```python
from ultralytics import YOLO

model = YOLO("runs/fire_yolo11n/weights/best.pt")

# save=True 会把画好框的图存到 runs/detect/predict/ 下
results = model.predict(
    source="samples/test.jpg",
    save=True,
    conf=0.25,        # 置信度阈值,低于它的框不显示;误检多就调高,漏检多就调低
    device=0,
)
for r in results:
    print("检测到的目标数:", len(r.boxes))
```

```bash
$PY scripts/predict_image.py
```

> 跑完去 `runs/detect/predict/` 看那张画了框的图。这是你第一次亲眼看到成果。

### 6.2 检测视频文件

把 `source` 换成视频路径即可:

```python
model.predict(source="samples/test.mp4", save=True, conf=0.25, device=0)
```

输出的带框视频同样在 `runs/detect/predict*/` 下。

### 6.3 实时摄像头 / 视频流

```python
# source=0 表示本机第一个摄像头;也可以填 RTSP 地址(无人机图传常用 RTSP)
model.predict(source=0, show=True, conf=0.25, device=0)
```

> `show=True` 会弹窗实时显示。以后无人机图传如果是 RTSP,把 `source` 换成
> `"rtsp://<无人机图传地址>"` 就能实时检测 —— 这就是接到无人机上的关键一步。

---

## 7. 步骤六:后续扩展(等前面都跑通再看)

跑通 1-5 后,才谈得上对接你的无人机实训系统。方向:

1. **接无人机图传流**:把 6.3 的 `source` 换成无人机的 RTSP 地址,实时检测。
2. **自动巡检报告**:检测到火/烟时,自动截图 + 记录时间戳 + (有 GPS 时)记录位置,
   汇总成一份报告(PDF / HTML)。这一步是纯 Python 逻辑,不难,到时我帮你写。
3. **报警触发**:置信度超过阈值持续 N 帧才报警,避免单帧误检乱报。
4. **模型提速**:如果要在无人机机载电脑(算力弱)上跑,可把模型导出成
   ONNX / TensorRT 格式加速。到时再说。

---

## 8. 一页速查(跑通全流程的命令顺序)

```bash
# 0. 每次开新终端先设这个
PY="C:/Users/nice/anaconda3/envs/fire-detect/python.exe"

# 1. 环境(只需做一次)
conda create -n fire-detect python=3.11 -y
$PY -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
$PY -m pip install ultralytics
$PY scripts/check_env.py          # 必须看到 CUDA 可用: True

# 2. 数据:下载 D-Fire 解压到 data/dfire/,写好 data/dfire.yaml

# 3. 训练
$PY scripts/train.py

# 4. 评估
$PY scripts/eval.py

# 5. 推理
$PY scripts/predict_image.py
```

---

## 9. 卡住了怎么办

任何一步报错,复制**完整的报错信息**发我,我帮你定位。
最容易卡的两个地方:
1. **第 2.5 步 CUDA 显示 False** —— torch 版本不对,重装 cu128 版。
2. **第 3 步数据集路径** —— yaml 里 `path` 必须是解压后的真实路径,斜杠用 `/`。

祝顺利 🔥

